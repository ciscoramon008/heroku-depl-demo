const express=require("express"),app=express(),PORT=process.env.PORT||3e3;app.set("view engine","ejs"),app.get("/",((e,s)=>{s.render("home")})),app.listen(PORT,(()=>{console.log(`Server has start on port ${PORT}`)}));
